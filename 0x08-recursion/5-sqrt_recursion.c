#include "main.h"

int checker(int checkPoint, int n)
{
	if (n < checkPoint)
	{
		return (-1);
	}
	return (n * n);
}
/**
 * _sqrt_recursion - Find the square-root of a number
 * @n: Number to find the square root
 *
 * Return: Square root of n
 */

int _sqrt_recursion(int n)
{
	int checkpoint;

	checkpoint = n / 2;


}
